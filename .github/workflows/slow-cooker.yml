name: Recipe Intelligence — Slow Cooker

on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/slow-cooker.yml'
      - '.github/workflows/_vertical-refresh.yml'
      - 'src/**'
      - 'tests/**'
      - 'config/verticals/slow_cooker/**'
      - 'config/slo.yaml'
      - 'requirements.txt'
      - 'requirements-dev.txt'
      - 'pyproject.toml'
      - 'verticals/slow_cooker/data/benchmarks/**'
  schedule:
    - cron: '31 * * * *'
    - cron: '7 9 * * *'
    - cron: '37 9 * * 0'
  workflow_dispatch:
    inputs:
      mode:
        description: Slow Cooker refresh mode
        required: true
        default: deep
        type: choice
        options: [hourly, daily, deep, backfill]
  pull_request:
    types: [opened, synchronize, reopened]
    paths:
      - '.github/workflows/slow-cooker.yml'
      - '.github/workflows/_vertical-refresh.yml'
      - 'src/**'
      - 'tests/**'
      - 'config/verticals/slow_cooker/**'
      - 'config/slo.yaml'
      - 'requirements.txt'
      - 'requirements-dev.txt'
      - 'pyproject.toml'
      - 'verticals/slow_cooker/data/benchmarks/**'

permissions:
  contents: write
  pages: write
  id-token: write

# Production runs share one repository-state mutation domain with Air Fryer,
# Source Expansion, and Source Catalog Sync. At most one production mutation runs
# at a time; newer queued work may coalesce an older pending run. Pull-request
# validation stays isolated per workflow/ref and does not block production.
concurrency:
  group: ${{ github.event_name == 'pull_request' && format('{0}-{1}', github.workflow, github.ref) || 'recipe-intelligence-production-state' }}
  cancel-in-progress: false

jobs:
  refresh:
    uses: ./.github/workflows/_vertical-refresh.yml
    with:
      vertical: slow-cooker
      mode: ${{ github.event_name == 'pull_request' && 'smoke' || github.event_name == 'workflow_dispatch' && inputs.mode || github.event.schedule == '7 9 * * *' && 'daily' || github.event.schedule == '37 9 * * 0' && 'deep' || 'hourly' }}
      production: ${{ github.event_name != 'pull_request' }}
    secrets: inherit
